# installation

## Requisites:
* `git` installed
* `npm` installed
* `mongodb` installed

1. clone the repo
2. cd into the repo
3. npm install
4. make sure `mongod` is running with: `systemctl status mongod`
5. 
